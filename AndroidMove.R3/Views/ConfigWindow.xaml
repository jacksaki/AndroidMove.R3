<mahApps:MetroWindow
    x:Class="AndroidMove.R3.Views.ConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dlg="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    xmlns:mahApps="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="clr-namespace:AndroidMove.R3.Views"
    xmlns:vm="clr-namespace:AndroidMove.R3.ViewModels"
    xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="設定"
    dlg:DialogParticipation.Register="{Binding}"
    v:DialogCloser.DialogResult="{Binding DialogResult}"
    Background="{DynamicResource MaterialDesignPaper}"
    BorderThickness="1"
    FontFamily="pack:application,,,/MaterialDesignThemes.Wpf;conponent/Resources/Roboto/#Roboto"
    GlowBrush="{DynamicResource AccentColorBrush}"
    ShowCloseButton="False"
    ShowMaxRestoreButton="False"
    ShowMinButton="False"
    SizeToContent="WidthAndHeight"
    Style="{StaticResource MaterialDesignWindow}"
    TextElement.FontSize="22"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    TitleCharacterCasing="Normal"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GroupBox Header="ADB設定" Style="{StaticResource MaterialDesignCardGroupBox}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="400" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Margin="4"
                    VerticalAlignment="Center"
                    Text="ADB パス" />
                <TextBox
                    Grid.Column="1"
                    Margin="4"
                    Text="{Binding AdbPath.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    Grid.Column="2"
                    Margin="4"
                    Command="{Binding AdbPathCommand}"
                    Content="…"
                    Style="{StaticResource MaterialDesignRaisedLightButton}" />
                <TextBlock
                    Grid.Row="1"
                    Margin="4"
                    VerticalAlignment="Center"
                    Text="監視間隔:" />
                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    Orientation="Horizontal">
                    <wpf:NumericUpDown
                        Margin="4"
                        HorizontalContentAlignment="Right"
                        Value="{Binding IntervalSeconds.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        Margin="4"
                        VerticalAlignment="Center"
                        Text="秒" />
                </StackPanel>
            </Grid>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="スクショ設定">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="ピクセルサイズ" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <wpf:NumericUpDown
                        Grid.Column="1"
                        Margin="4"
                        HorizontalContentAlignment="Right"
                        Minimum="200"
                        Value="{Binding PixelSize.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        Text="px" />
                </StackPanel>
                <TextBlock
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    Text="幅/高さ" />
                <ToggleButton
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    wpf:ToggleButtonAssist.OnContent="高さ"
                    Content="幅"
                    IsChecked="{Binding WidthSelected.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Style="{StaticResource MaterialDesignActionLightToggleButton}" />
            </Grid>
        </GroupBox>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="1"
                Margin="4"
                Command="{Binding SaveCommand}"
                Content="{wpf:PackIcon Kind=Floppy,
                                       Size=24}"
                Style="{StaticResource MaterialDesignRaisedLightButton}" />
            <Button
                Grid.Column="2"
                Margin="4"
                Command="{Binding CancelCommand}"
                Content="{wpf:PackIcon Kind=Cancel,
                                       Size=24}"
                Style="{StaticResource MaterialDesignRaisedLightButton}" />
        </Grid>

    </Grid>
</mahApps:MetroWindow>
